
# VIM setup
export VISUAL=nvim
export EDITOR=nvim
alias vi="nvim"
alias vim="nvim ."

# Utility commands
alias ..="cd .."
alias ll="ls -al"
alias la="ls -a"

bindkey -s ^f "bash ~/.local/scripts/tmux-sessionizer.bash\n"

